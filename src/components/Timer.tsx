import React from 'react'
import { View, Text } from 'react-native'
import { GLOBAL_STYLES as STYLES } from '../styles'

const Timer = () => <View style={STYLES.standard}><Text>Timer</Text></View>

export default Timer 